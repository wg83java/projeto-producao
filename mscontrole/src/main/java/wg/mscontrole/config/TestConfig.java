package wg.mscontrole.config;

import org.springframework.context.annotation.Configuration;

import lombok.RequiredArgsConstructor;

@Configuration
@RequiredArgsConstructor
public class TestConfig {
	
	
	/*@Bean
	public List<OrdemPedido> test() {
		
		OrdemPedido ordem = new OrdemPedido(null,"cbc16mm",10);
		OrdemPedido ordem2 = new OrdemPedido(null,"cb25mm",20);
		
		return repository.saveAll(Arrays.asList(ordem,ordem2));
	}*/

}
